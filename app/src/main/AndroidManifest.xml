<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.athletica">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.facility.CommentAdapter"></activity>
        <activity android:name=".View.settings.ChangePasswordActivity" />
        <activity android:name=".View.maps.MapsActivity" />
        <activity android:name=".View.search.DisplayAll" />
        <activity android:name=".View.event.ViewEventActivity" />
        <activity android:name=".View.login.LoginRegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.profile.CreateProfileActivity" />
        <activity android:name=".View.facility.ViewFacilityActivity" />
        <activity android:name=".View.event.CreateEventActivity" />
        <activity android:name=".View.search.SearchResultActivity"></activity>
        <activity android:name=".View.settings.SettingsActivity" />
        <activity android:name=".View.profile.ViewProfileActivity" />
        <activity android:name=".View.home.HomeActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCuq6CEFfhi2ndZPjupSEMh6mqntJ1bYOo" />
    </application>

</manifest>